{
  "name": "@getpaseo/browser-companion",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run src/companion.test.ts --bail=1",
    "test:browser": "npm run build && node scripts/smoke.mjs"
  },
  "dependencies": {
    "@getpaseo/protocol": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "esbuild": "^0.28.2",
    "playwright": "^1.56.1",
    "vitest": "^4.1.6"
  }
}
